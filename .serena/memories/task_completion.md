# After finishing a task

1. Only `local/` + agreed pages. No Bitrix kernel.
2. UTF-8. `php -l` if CLI exists.
3. Confirm Bitrix API via MCP.
4. PhpStorm auto-uploads to live `kv04` — mention if relevant. Prefer scp to `infopolura@77.222.40.47` when auto-upload lags.
5. Do **not** update `docs/specs/` or `.serena/memories/` unless the user asks.
