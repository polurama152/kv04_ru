// Единственный файл дневника на модульном синтаксисе. pdf.js с четвёртой версии
// поставляется только как ESM, а весь остальной наш код — обычные скрипты в
// стиле ES5. Шим стоит между ними: браузер грузит его как
// <script type="module">, а наружу выставляет привычный глобальный объект.
//
// Отдельным файлом, а не inline-скриптом: инлайн упёрся бы в CSP, случись ей
// появиться, и не давал бы кэшировать себя вместе с библиотекой.
import * as pdfjsLib from './pdf.min.js';

window.pdfjsLib = pdfjsLib;
