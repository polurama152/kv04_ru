# Документация kv04.ru

**Сначала:** [ONBOARDING.md](ONBOARDING.md) — карта решения для нового агента.

Два слоя, не смешивать:

| Слой | Где | Зачем |
|------|-----|--------|
| **Правила** | `.cursor/rules/*.mdc` | Как писать код в каждом чате |
| **Спеки** | `docs/specs/` | Что решили, что сделано, какой долг |

Код сайта — `public_html/`. Кастом — `public_html/local/`. Ядро Bitrix не трогаем. Модули готовим к Маркетплейсу (`kv04.*`).

Онбординг:

1. `docs/ONBOARDING.md`
2. `.cursor/rules/kv04-core.mdc`
3. `docs/specs/README.md` и спека `0001`
4. Bitrix MCP для API, не grep ядра

Спеки и `.serena/memories/` править только по явной команде пользователя.
