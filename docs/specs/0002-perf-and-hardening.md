# 0002 — Производительность и версионирование

- Статус: `in-progress`
- Дата: 2026-08-21, обновлено 2026-08-23
- Модуль: `kv04.diary`, инфраструктура репозитория

## Зачем

Приоритет продукта — мгновенный отклик при любом объёме контента и числе
пользователей. Замеры показали, что упирается не в объём данных (12 заметок),
а в накладные расходы на каждый запрос. Плюс проект не имел версионирования
при `autoUpload=Always`: любая правка уезжала на прод без возможности отката.

## Решение

**Версионирование.** `git init`, ветка `main`, remote
`git@github.com:polurama152/kv04_ru.git`. Из репозитория исключены ядро
Bitrix (5.7 ГБ), `upload/`, дампы, `cgi-bin/` (28 МБ бинарников прежнего
хостинга) и всё, что содержит пароли: `.settings.php`, `dbconn.php`.
`.gitattributes` держит LF — сервер Linux.

**`Installer::ensure()`.** Вызывался дважды за запрос (`index.php` и
`executeComponent()` обоих компонентов) и каждый раз выполнял ~10 запросов
старым API плюс два безусловных `INSERT INTO b_option`. Каждый показ главной,
включая гостевой, писал в базу; под конкурентной нагрузкой это упёрлось бы в
блокировку двух одних и тех же строк.

- статический флаг `$ensured` — повторный вызов в том же запросе бесплатен;
- опция `schema_version` — выход без единого запроса, если схема применена;
- `setOption()` сверяет значение перед записью;
- `ensure(true)` — принудительное переприменение;
- идентификаторы HL и инфоблока проверяются отдельно, поэтому ручное удаление
  сущностей приводит к повторному созданию, а не к тихому пропуску.

Вызовы в компонентах оставлены: модуль должен быть самодостаточным у клиента
Маркетплейса, а стоимость повторного вызова теперь нулевая.

## Вне скоупа

N+1 в `NoteService::list()`, счётчик `ipfail_*` в `b_option`,
локальный highlight.js, переезд с shared-хостинга — вынесено в долг ниже.
Первые три с тех пор закрыты, см. «Долг»; переезд остаётся.

## Контракт

Новая опция модуля `kv04.diary`: `schema_version` (строка, на 2026-08-23
значение `6`). Поднимать `Installer::SCHEMA_VERSION` при изменении структуры HL или
инфоблока — это заставит `ensure()` один раз переприменить схему.

Сигнатура: `Installer::ensure(bool $force = false): void`. Поведение при
`$force = false` и применённой схеме — no-op.

## Замеры

Сняты на сервере (`php8.4`, боевая база, `curl` с `127.0.0.1` — без сети).

| Что | Было | Стало |
|-----|------|-------|
| `Installer::ensure()`, схема применена | 14–16 мс | 0.06 мс |
| Он же, повторный вызов в запросе | 14–16 мс | 0.000 мс |
| Записей в `b_option` на запрос | 2–4 | 0 |
| Главная, TTFB | 105–130 мс | 93–108 мс |

### Из чего складывается отклик

Замерено через веб-SAPI (mod_php с opcache), а не CLI — цифры боевые.

| Слой | Стоимость | Чем убирается |
|------|-----------|---------------|
| nginx → Apache → mod_php, до первой строки PHP | **29–32 мс** | только сменой хостинга на nginx + PHP-FPM |
| `prolog_before` ядра Bitrix | **25–35 мс** | только отказом от Bitrix на этом запросе |
| Всё остальное: компоненты, `NoteService`, шаблон | ≈40 мс | оптимизацией нашего кода |

Отдельно проверено: «облегчённый» пролог с `NO_KEEP_STATISTIC`,
`NO_AGENT_CHECK`, `DisableEventsCheck`, `PUBLIC_AJAX_MODE` и
`BX_SKIP_SESSION_TERMINATE` даёт **25–29 мс против 25–35 мс** — в пределах
погрешности, выигрыша по медиане нет. Смысл у `NO_AGENT_CHECK` остаётся
только как страховка хвоста: без него на случайный запрос может выпасть
прогон агентов Bitrix.

Не узкие места: TLS-хендшейк сервера 6–10 мс, MySQL 0.03 мс/запрос,
`innodb_buffer_pool` 8 ГБ.

### Чистка от 2026-08-21

| | Было | Стало |
|---|------|-------|
| `public_html/` | 5.7 ГБ | **2.5 ГБ** |
| Квота на сервере | 11 455 МБ | **8 134 МБ** |
| Файлов (inode) | 427 тыс. | **296 тыс.** |

Удалены архивы из `bitrix/backup/` (1.88 ГБ, октябрь 2025) вместе с
распакованной туда копией сайта (54 МБ) и `bitrix/tmp/restore.removed/`
(1.4 ГБ — вытесненное июньским восстановлением старое ядро: 45 модулей
против 57 живых). `bitrix/tmp/bitrix/` (52 КБ) оставлен — рабочий скрэтч.

`bitrix/backup/.htaccess` восстановлен: `Require all denied` для Apache 2.4
плюс `Deny from all` для 2.2, `Options -Indexes`, `php_flag engine off`.
Проверено канарейкой — реальный файл в каталоге отдаёт 403.


## Файлы

- `.gitignore`, `.gitattributes` — корень репо
- `public_html/local/modules/kv04.diary/lib/installer.php`

## Ловушка, на которой уже обожглись

Быстрый путь `Installer::ensure()` убрал не только запросы, но и побочный
эффект: раньше метод на каждом запросе вызывал `Loader::includeModule('iblock')`,
и `NoteService` молча этим пользовался. После оптимизации лента залогиненного
пользователя падала с `Class "CIBlockElement" not found` — то есть HTTP 500 и
«Нет связи» в интерфейсе. Гостевую страницу это не задевало, поэтому проверка
главной ничего не показала.

Барьер `NoteService::requireIblock()` теперь стоит **первой строкой каждого
публичного метода**. Внутри `iblockId()` его недостаточно: PHP резолвит класс
`CIBlockElement` раньше, чем вычисляет аргументы вызова, поэтому
`CIBlockElement::GetList(..., self::iblockId(), ...)` падал бы до срабатывания
проверки.

Вывод на будущее: убирая вызов из горячего пути, проверять не только время, но
и то, что этот вызов делал попутно. И проверять оба состояния — гостя и
залогиненного, — они идут разными ветками кода.

## Долг

Закрытое помечено датой и не удаляется: видно, что уже проверено и чем
кончилось.

**Безопасность.**

- ~~`/bitrix/backup/*.sql` и `*.tar.gz` скачиваются публично~~ — **закрыто
  2026-08-21.** Причина: восстановление 2026-06-27 распаковало архив прямо в
  `bitrix/backup/`, и корневой `.htaccess` из архива лёг поверх штатного
  `Deny from All`. Архивы удалены, защита восстановлена, проверено канарейкой.
- ~~Публично доступны отладочные скрипты в корне~~ — **закрыто 2026-08-22.**
  Семь файлов отвечали 200. Опаснее прочих `bitrixsetup.php` — штатный
  установщик Bitrix с блоком настроек прокси, то есть чужой путь записи на
  сервер; в репозитории его не было, приехал с тем же восстановлением 27.06.
  Остальные: `check_tables.php` (отчёт по таблицам базы), `test.php` и
  `check_encoding.php` (коннект к базе с `display_errors = 1`),
  `bitrix_test.php` (ядро с `NOT_CHECK_PERMISSIONS`), `index_.php`,
  `enum_test.php`. Вынесены за document root, из репозитория удалены. В
  `.htaccess` правило: те же имена в корне отдают 403, даже если файл вернётся
  с очередным восстановлением. Синтаксис mod_rewrite, а не `Require all
  denied`: на площадке Apache 2.2.29. Проверено канарейкой — реально лежащий в
  корне `test.php` отдаёт 403, а `about/test.php` остаётся 200.
- ~~`PinService::create()` не проверяет блокировку по IP и отвечает «Такой пин
  занят»~~ — **закрыто.** Идентичностью стала почта: `create()` сверяется с
  лестницей по IP, а занятость сообщает про почту, а не про пин. Чужой адрес
  войти не помогает, и каждая попытка стоит места в лестнице.
- ~~`PinService::registerPinFail()` не вызывается ниоткуда~~ — **закрыто.**
  Метода больше нет, счётчики ведёт `AttemptLimiter` в своей таблице.
- ~~Успешный вход сбрасывает счётчик IP~~ — **закрыто.** Сбрасывается только
  счётчик аккаунта; счётчик по IP переживает удачный вход, иначе владелец
  перебирал бы чужие пины по две догадки за цикл.
- ~~`session.use_strict_mode = 0` на хостинге~~ — **закрыто 2026-08-23.**
  Фиксация в лоб и не воспроизводилась: настройка стояла в `Off`, но ядро
  Bitrix перевыпускало незнакомые идентификаторы — проверено дважды, в том
  числе id верного формата. Теперь строгий режим включён и на уровне PHP.
- ~~Файлы сессий лежали в общем `/var/tmp`~~ — **закрыто 2026-08-23.** Это
  оказалось опаснее строгого режима. В каталоге 684 тысячи файлов сессий всех
  клиентов машины. Содержимое чужих закрыто правами `0600` — перебрал две
  сотни, ни один не читается, — но **имена файлов видны всем**, а имя файла и
  есть идентификатор сессии, то есть весь секрет. Дневник пускает по сессии
  (`Auth::getOwnerId()` сначала смотрит `$_SESSION`), привязки к IP или
  браузеру нет: проверено на своей же сессии — сервер принимает её id от
  любого клиента. Сессии переехали в `/home/i/infopolura/sessions`, права
  `drwx------`.
- ~~`http://kv04.ru` отвечал 200 без редиректа на https~~ — **закрыто
  2026-08-23.** Cookie сессии могла уехать по сети открытым текстом. Теперь
  301 на https с сохранением пути, параметров и `www`, а на cookie сессии и на
  обеих cookie дневника стоят `secure`, `HttpOnly`, `SameSite=Lax`.
- **Apache 2.2.29 на площадке**, сборка 2021 года. Ветка закрыта
  разработчиками в 2018-м, обновлений безопасности по ней нет. Из-за неё же в
  `.htaccess` не работает синтаксис 2.4.

**Производительность.**

- ~~`NoteService::list()` — N+1~~ — **закрыто 2026-08-23.** Файлы всех заметок
  берутся одним `SELECT ... IN (...)` из той же таблицы, куда пишет
  `setMediaProperty()`; чтение стало симметрично записи. Цифры ниже.
- ~~`ipfail_*` в `b_option` растёт неограниченно~~ — **закрыто.** Счётчики
  переехали в `kv04_diary_attempts`, в опциях модуля их больше нет.
- ~~`location.reload()` после сохранения~~ — **убрано 2026-08-21.** Сервер
  отдаёт готовый элемент (`NoteService::get()`), фронт дорисовывает его сам;
  правка подставляет пришедший санитизированный HTML вместо textarea.
  Компонент больше не перечитывает всю ленту после `add`. Перезагрузка
  осталась только на выходе из дневника, где она уместна.
- ~~highlight.js с cdnjs блокирующим `<script>`~~ — **закрыто 2026-08-23.**
  Библиотека и тема лежат в модуле, версия та же (11.9.0), файлы сверены с
  суммами sha512 из API cdnjs. Скрипт по-прежнему блокирующий, но со своего
  домена: лишних DNS и TLS к третьему домену нет.
- Сессии в файлах — блокировка сериализует параллельные запросы одного
  посетителя. Замер 2026-08-23: два одновременных запроса с одной сессией —
  0.105 с, с разными — 0.084 с, одиночный — 0.080 с. Папку унесли из общего
  `/var/tmp`, но файловая природа осталась; лечится Redis, то есть переездом.
- ~~3.4 ГБ мусора в document root~~ — **вычищено 2026-08-21**, освобождено
  3.3 ГБ и 131 тыс. inode. Общий массив хостинга по-прежнему занят на 98% —
  это не наша квота, а риск площадки.

**Записано было неверно — проверено замером 2026-08-23.**

- «`CFile::GetFileArray` на каждый файл, до ~500 запросов на полную ленту» —
  на самом деле ноль запросов: файлы приходят из кэша Bitrix. N+1 в ленте был,
  но ровно на свойстве MEDIA, по запросу на заметку.
- «Статику отдаёт Apache, а не nginx» — файлы модуля отдаёт nginx
  (`Server: nginx/1.31.3`). А вот HTTP/2 действительно нет, отдача идёт по
  HTTP/1.1.

### Замеры 2026-08-23

`php8.4`, боевая база, лента из 7 заметок с 4 файлами.

| Что | Было | Стало |
|-----|------|-------|
| `NoteService::list()`, повторный вызов | 8 запросов, 2.1 мс | 2 запроса, 0.8 мс |
| Он же, холодный | 25 запросов, 32 мс | 20 запросов, 34 мс |
| Чтение MEDIA на ленте из 100 заметок | 100 запросов, 7.0 мс | 1 запрос, 0.14 мс |

Холодный вызов почти не сдвинулся, и это честно: остаток — разовая
инициализация инфоблока, она не зависит от числа заметок. Смысл правки в
другом — счётчик запросов перестал расти вместе с дневником.

Равнозначность проверена отпечатком ленты до и после правки: id заметок, даты,
длины текстов, id и типы файлов, порядок прикрепления, корзина со сроками —
совпало у обоих владельцев до строчки.

### Ловушка площадки: версия PHP

Ядро Bitrix содержит `enum` (`main/lib/Messenger/Config/WorkerRunMode.php`),
поэтому сайт требует PHP 8.1 и выше. Переключение площадки на 8.0 22.08.2026
положило сайт целиком: `ParseError` при конструировании
`Bitrix\Main\Application`, 500 на каждой странице, включая витрину магазина.
Возврат на 8.4 всё чинит. Смена версии PHP здесь — не безобидная галочка, и
логов PHP у сайта на площадке нет: ошибка видна только в теле ответа.

### Ловушки площадки: mod_php и схема запроса

- **PHP работает модулем Apache**, а не CGI и не FPM. Значит `.user.ini` здесь
  не читают вовсе — он только для CGI и FPM. Настройки сессий, положенные
  туда, молча не применились: пять минут ждал истечения кэша, значения не
  менялись. Работает `php_value` / `php_flag` в `.htaccess`. Обратная примета:
  если `php_value` в `.htaccess` не роняет сайт пятисоткой — значит mod_php.
- **Apache не знает, что запрос пришёл по https.** nginx терминирует TLS и
  ходит к нему обычным http, поэтому `%{HTTPS}` пуст, `REQUEST_SCHEME` равен
  `http`, а `SERVER_PORT` — 80 даже на https-запросе. Единственный признак —
  заголовок `X-Forwarded-Proto`. Редирект по `%{HTTPS}` зациклил бы сайт
  насмерть.
- После редиректа запросы по петле (`curl` на `127.0.0.1`) надо слать с
  `-H "X-Forwarded-Proto: https"`, иначе в ответ приходит 301, а не страница.

**Инфраструктура.** Shared-хостинг Beget — потолок для цели «мгновенный
отклик». Redis и Memcached недоступны, кэш Bitrix файловый. Замер 2026-08-23:
сервер отдаёт главную за 79–93 мс изнутри, а до браузера ответ идёт 0.77–1.3 с
при load average 9 на общей машине — транспорт и соседи по железу стоят
дороже, чем весь PHP. Следующий шаг — VPS 4 vCPU / 8 ГБ / NVMe: nginx +
PHP-FPM без Apache, Redis под сессии и кэш, HTTP/2 и HTTP/3. Ожидаемо
120 мс → 25–40 мс.
